<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App1"
             x:Class="App1.Page">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:UriToUrlInfoConverter x:Key="ToUrlInfoConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <local:UrlInfoBox Element="{Binding Source={x:Reference Name=Url}, Path=Text, Converter={StaticResource ToUrlInfoConverter}}" Grid.Row="0"/>
            <StackLayout Grid.Row="1">
                <local:UrlTextBox x:Name="Url"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>