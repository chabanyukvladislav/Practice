<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:App1"
             x:Class="App1.Page">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:UriToUrlInfoConverter x:Key="ToUrlInfoConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <local:UrlInfoBox Element="{Binding Source={x:Reference Name=Url}, Path=UrlText, Converter={StaticResource ToUrlInfoConverter}}" Grid.Row="0"/>
            <StackLayout Grid.Row="1">
                <local:UrlTextBox x:Name="Url" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>